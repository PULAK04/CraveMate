# CraveMate Frontend

React + TypeScript + Vite frontend for CraveMate.

## Development

1. Copy `.env.example` to `.env` and set Google/Stripe keys plus service URLs.
2. Install dependencies: `npm install`
3. Start development server: `npm run dev`
4. Production build: `npm run build`

The full architecture, feature list, environment rules, and backend startup order are documented in the repository root `README.md`.
